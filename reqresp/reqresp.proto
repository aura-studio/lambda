syntax = "proto3";
package reqresp;
option go_package = "github.com/aura-studio/lambda/reqresp";

message Request {
  string path = 1;            // 请求路径
  bytes payload = 2;          // 请求数据
}

message Response {
  bytes payload = 1;          // 响应数据
  string error = 2;           // 错误信息
}
